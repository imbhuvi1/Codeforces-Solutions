#include <stdio.h>
#include<math.h>
int main()
{
    int n,m,a;
    scanf("%d %d %d",&n,&m,&a);
    
    long long int t = ceil(((double)n)/a)*ceil(((double)m)/a);
    printf("%lld",t);
    return 0;
}
